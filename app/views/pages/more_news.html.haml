%h2= t('latest_news.more_news')
- NewsItem.all.each do |news_item|
  %h4= t news_item.date.to_s(:short)
  %p= news_item.text
  - if news_item != @news_items.last
    .separation{:style => "text-align: center;"}
      %i{:class => "fa fa-square"}
      %i{:class => "fa fa-square"}
      %i{:class => "fa fa-square"}